"""Persistence layer — SQLAlchemy models, session management, and
repositories.

Step 2 (this step): async engine + session factory (``database.py``),
declarative base (``base.py``), and the per-request session dependency
plus a connectivity check (``session.py``). No ORM models yet — those
start in Step 3 (Trade CRUD), together with Alembic migrations."""
